https://colab.research.google.com/drive/1EDCLCXZEVhVAn8OPY1aaMKbgM5KUMSJC?usp=sharing#scrollTo=uQ6J5QFnQien
شما احتمالاً منظورتان **ساختن یک محیط مجازی (Virtual Environment)** برای پروژه‌های **پایتون** در لینوکس است. محیط‌های مجازی به شما این امکان را می‌دهند که وابستگی‌ها (dependency) و نسخه‌های مختلف کتابخانه‌ها را برای هر پروژه به طور جداگانه مدیریت کنید.

در اینجا مراحل ساخت محیط مجازی پایتون در لینوکس با استفاده از ماژول استاندارد `venv` آمده است:

-----

## 🛠️ ۱. اطمینان از نصب Python و `venv`

ابتدا مطمئن شوید که **پایتون ۳** و ماژول `venv` روی سیستم شما نصب شده‌اند.

  * برای بررسی نسخه پایتون:
    ```bash
    python3 --version
    ```
  * اگر ماژول `venv` نصب نبود (که در برخی توزیع‌های لینوکس نیاز به نصب جداگانه دارد)، می‌توانید آن را نصب کنید. مثلاً در توزیع‌های مبتنی بر دبیان/اوبونتو:
    ```bash
    sudo apt update
    sudo apt install python3-venv
    ```
    در توزیع‌های مبتنی بر فدورا/سنت‌اواس:
    ```bash
    sudo dnf install python3-virtualenv
    ```

-----

## 🏗️ ۲. ساخت محیط مجازی

به پوشه پروژه‌تان بروید (یا یک پوشه جدید بسازید) و دستور زیر را اجرا کنید. در اینجا، نام محیط مجازی را `my_env` فرض می‌کنیم. می‌توانید هر نام دیگری را انتخاب کنید (معمولاً از `venv` استفاده می‌شود):

```bash
python3 -m venv my_env
```

  * **توضیح:** این دستور یک پوشه به نام `my_env` در مسیر فعلی ایجاد می‌کند که شامل یک نسخه مجزا از مفسر پایتون، کتابخانه‌های استاندارد و ابزارهایی مانند `pip` است.

-----

## 🚀 ۳. فعال‌سازی محیط مجازی

برای اینکه از محیط مجازی استفاده کنید، باید آن را **فعال** (Activate) کنید.

```bash
source my_env/bin/activate
```

  * **علامت تشخیص:** پس از فعال‌سازی، نام محیط مجازی (در اینجا `(my_env)`) در ابتدای خط فرمان (Prompt) شما ظاهر می‌شود. این یعنی شما اکنون درون محیط ایزوله قرار دارید.
  * **نکته:** تا زمانی که محیط فعال است، هر کتابخانه‌ای که با `pip install` نصب کنید، فقط در این محیط (`my_env`) نصب می‌شود و روی پایتون سیستمی شما تأثیری نمی‌گذارد.

-----

## 📦 ۴. نصب کتابخانه‌ها

اکنون می‌توانید هر کتابخانه‌ای را که پروژه شما نیاز دارد، نصب کنید:

```bash
pip install package_name
# مثال: pip install requests
```

-----

## 🚪 ۵. غیرفعال‌سازی محیط مجازی

وقتی کارتان با پروژه تمام شد و خواستید به پایتون سیستمی اصلی برگردید، محیط مجازی را **غیرفعال** (Deactivate) کنید:

```bash
deactivate
```

  * **علامت تشخیص:** نام محیط مجازی از ابتدای خط فرمان شما حذف می‌شود.

-----

**آیا به دنبال ساخت یک نوع محیط دیگر، مانند محیط کانتینری با Docker یا محیط‌های مبتنی بر Conda، هستید؟**
